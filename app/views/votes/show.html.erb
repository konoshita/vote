<p style="color: green"><%= notice %></p>
<div class="album py-5 bg-light">
    <div class="container">
      <h1></h1>
        <div class="row justify-content-center">
          <div class="col-md-8">
          <%= render 'full_vote' ,{ vote: @vote}%>
          </div>
      </div>
    </div>
      <%= render 'answer_new' %>
    </div>  
</div>
