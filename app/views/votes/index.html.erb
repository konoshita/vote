<p style="color: green"><%= notice %></p>
  <div class="album py-5 bg-light">
    <div class="container">
      <div class="row  row-cols-1 row-cols-md-3 g-4 mb-3 ">
      <% @votes.each do |vote| %>
        <%= render vote %>
        <%= link_to "回答する", vote_path(vote), class:"btn btn-primary my-2" %>
        <%= link_to "集計", edit_vote_path(vote), class:"btn btn-outline-secondary my-2" %>
            </div>
          </div>
        </div>
      <% end %>
          </div>
        </div>
  </div>
